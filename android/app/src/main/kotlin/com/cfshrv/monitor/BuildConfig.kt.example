package com.cfshrv.monitor

/**
 * Build configuration - Copy this to BuildConfig.kt and set your local IP
 *
 * IMPORTANT: BuildConfig.kt is git-ignored, so your IP won't be committed
 */
object BuildConfig {
    // For Android Emulator, use: "https://10.0.2.2:4777/api"
    // For physical device on same WiFi, use: "https://YOUR_COMPUTER_IP:4777/api"
    const val API_BASE_URL = "https://10.0.2.2:4777/api"
}
